test_files = [
    'mainSection',
    'noSection',
    'script_section'
]

foreach file : test_files
    configure_file (
        input : file,
        output : file,
        copy: true
    )
endforeach

